var fun = function (list,element,interface) {
  list.push(element);
  interface.call();
};

// list.push(12313);
// salesOffices.trigger(list, [123456,78979]);
// salesOffices.trigger(list, [222,111]);
// salesOffices.trigger(list, [121,212,85666]);
// salesOffices.trigger('squareMeter100', 3000000);
